<<<<<<< HEAD
name: Codacy CI

on:
  push:
    branches:
      - 'feature_task_*'   # Se ejecuta en cualquier rama de feature
      - 'trunk'            # También en trunk para validación final
      - 'main'             # Ahora también en main

jobs:
  build:
=======
name: test-and-coverage

on:
  push:
    branches: [ trunk ]

jobs:
  test-and-coverage:
>>>>>>> origin/trunk
    runs-on: ubuntu-latest

    services:
      mysql:
<<<<<<< HEAD
        image: mysql:5.7
        env:
          MYSQL_ROOT_PASSWORD: uvlhub_root_password
          MYSQL_DATABASE: uvlhubdb_test
          MYSQL_USER: uvlhub_user
          MYSQL_PASSWORD: uvlhub_password
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    strategy:
      matrix:
        python-version: ['3.11', '3.12']

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check for outdated dependencies
        run: |
          pip list --outdated

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
=======
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: default_db
          MYSQL_USER: default_user
          MYSQL_PASSWORD: default_password
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    env:
      DB_USER: default_user
      DB_PASSWORD: default_password
      DB_NAME: default_db
      DB_HOST: 127.0.0.1
      DB_PORT: 3306
      DATABASE_URL: mysql+pymysql://default_user:default_password@127.0.0.1:3306/default_db

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
>>>>>>> origin/trunk

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
<<<<<<< HEAD
=======
<<<<<<< HEAD

      - name: Security audit with pip-audit
        run: |
          pip install pip-audit
          pip-audit || true

      - name: Upload coverage to Codacy
        run: |
          pip install codacy-coverage
          coverage run -m pytest app/modules/ --ignore-glob='*selenium*'
          coverage xml
          python-codacy-coverage -r coverage.xml
        env:
          FERNET_KEY: ${{ secrets.FERNET_KEY }}
          FLASK_ENV: testing
          MARIADB_HOSTNAME: 127.0.0.1
          MARIADB_PORT: 3306
          MARIADB_TEST_DATABASE: uvlhubdb_test
          MARIADB_USER: uvlhub_user
          MARIADB_PASSWORD: uvlhub_password
=======
          # Si tienes otro fichero (requirements-dev.txt, etc.), añádelo aquí
>>>>>>> 60e60f3715d5b2d66af5f212768c480611e6dcdb

      - name: Wait for MySQL
        run: |
          for i in {1..30}; do
            if mysqladmin ping -h127.0.0.1 -uroot -proot --silent; then
              echo "MySQL is up"
              break
            fi
            echo "Waiting for MySQL..."
            sleep 2
          done

      - name: Run tests with coverage (skip Selenium always)
        run: |
          pytest --cov=app --cov-report=xml \
            -k "not selenium" \
            --ignore=tests/selenium \
            --ignore-glob="*selenium*" \
            --ignore-glob="*Selenium*" \
            --ignore-glob="*SELENIUM*"

      - name: Upload coverage to Codacy
        if: always()
        run: |
          pip install codacy-coverage
          python-codacy-coverage -r coverage.xml
        env:
>>>>>>> origin/trunk
          CODACY_PROJECT_TOKEN: ${{ secrets.CODACY_PROJECT_TOKEN }}
